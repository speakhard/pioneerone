---
schema: devtracker/session-log@1
project: pioneerone
session_id: de5f5c4d-1e9e-40f5-8d55-6d1faa5219ed
producer: devtracker
trigger: idle
ended_at: '2026-08-15T19:32:24Z'
git:
  branch_end: main
  head_end: 879437a
  head_subject: Never publish a contact address nobody has checked
files_modified:
  - content/episodes.toml
  - static/style.css
  - templates/base.html
  - templates/story.html
  - templates/archive.html
  - static/site.js
  - assets-src/SOURCES.md
  - STORY-SOURCES.md
  - tests/test_build.py
  - README.md
  - serve.py
  - shoot.py
  - prepare_assets.py
  - tests/test_interaction.py
  - PARKING_LOT.md
  - /home/fs42/Development/workspace/reports/pioneerone-mvp-2026-08-15.md
  - content/site.toml
  - builder.py
  - templates/index.html
  - deploy/README.md
tests:
  - command: "cat > requirements.txt <<'EOF'\n# The build needs a template engine and nothing else.\nJinja2==3.1.6\nEOF\ncat > requirements-dev.txt <<'EOF'\n-r requirements.txt\n# Deriving images from assets-src/ (prepare_assets.py) — not needed to deploy.\nPillow==10.2.0\n# Rendering the built site at device sizes (shoot.py) — not needed to deploy.\nplaywright==1.49.0\npytest==8.3.4\nEOF\ncat > .gitignore <<'EOF'\n.venv/\n__pycache__/\n*.pyc\n.pytest_cache/\n\n# Generated by builder.py on every build, including at deploy time"
    outcome: unknown
    passed:
    failed:
    detail: "written\nassets-src\nbuilder.py\ncontent\ndeploy\nprepare_assets.py\n__pycache__\nrequirements-dev.txt\nrequirements.txt\nshoot.py\nsite\nstatic\nSTORY-SOURCES.md\ntemplates\ntests"
  - command: tail -12 /tmp/claude-1000/-home-fs42/de5f5c4d-1e9e-40f5-8d55-6d1faa5219ed/tasks/b1pn98q3e.output 2>/dev/null; echo "---tests---"; .venv/bin/python -m pytest -q 2>&1 | tail -25
    outcome: failed
    passed: 9
    failed: 3
    detail: 3 failed, 9 passed
  - command: .venv/bin/python -m pytest tests/test_build.py::test_every_referenced_asset_exists -q 2>&1 | grep -A15 "referenced but not built"
    outcome: failed
    passed:
    failed: 1
    detail: 1 failed
  - command: "python3 - <<'PY'\nimport pathlib\n\n# --- real bug: autoescape mangled the JSON-LD into &#34; entities ---\nb=pathlib.Path(\"builder.py\"); t=b.read_text()\nt=t.replace('''            \"sameAs\": [watch[\"channel_url\"], watch[\"archive_org_url\"]],\n        },\n        indent=2,\n    )''','''            \"sameAs\": [watch[\"channel_url\"], watch[\"archive_org_url\"]],\n        },\n        indent=2,\n    ).replace(\"<\", r\"\\\\u003c\")  # cannot break out of the <script> that carries it''')\nb.write_text(t)\n\ni=pathlib.Path(\"t"
    outcome: passed
    passed: 12
    failed:
    detail: 12 passed
  - command: "python3 - <<'PY'\nimport pathlib\n\n# builder: expose the portrait srcset alongside the landscape one\nb=pathlib.Path(\"builder.py\"); t=b.read_text()\nt=t.replace('    hero_widths = image_widths(\"hero\")',\n            '    hero_widths = image_widths(\"hero\")\\n    hero_portrait_widths = image_widths(\"hero-portrait\")')\nt=t.replace('            hero_srcset=srcset(\"hero\", hero_widths, rel),',\n            '            hero_srcset=srcset(\"hero\", hero_widths, rel),\\n            hero_portrait_srcset=srcset(\"her"
    outcome: passed
    passed: 12
    failed:
    detail: 12 passed
  - command: "python3 - <<'PY'\nimport pathlib, re\n\n# The mobile hero is now a landscape band, so the portrait crop has no user.\np=pathlib.Path(\"prepare_assets.py\"); t=p.read_text()\nt=t.replace(\"\"\"    # A phone hero is roughly 393x800. object-fit: cover on a 16:9 frame at that\n    # shape shows about a quarter of the image's width — enough zoom that the\n    # patch stops being a patch and becomes a red smear. A 3:4 crop is close\n    # enough to the viewport that the frame survives, so phones get their own.\n   "
    outcome: passed
    passed: 12
    failed:
    detail: 12 passed
  - command: .venv/bin/python -m pytest tests/test_interaction.py -q 2>&1 | tail -30
    outcome: error
    passed:
    failed:
    detail: '/bin/bash: line 1: .venv/bin/python: No such file or directory'
  - command: cd /home/fs42/Development/pioneerone && .venv/bin/python -m pytest tests/test_interaction.py -q 2>&1 | tail -30
    outcome: passed
    passed: 5
    failed:
    detail: 5 passed
  - command: "python3 - <<'PY'\nimport pathlib\nc=pathlib.Path(\"static/style.css\"); t=c.read_text()\nold=\"  --paper-faint: #6f6b66; /* captions, metadata */\"\nnew=\"  --paper-faint: #817d78; /* captions, metadata — 4.5:1 on both grounds */\"\nassert old in t\nt=t.replace(old,new)\nc.write_text(t); print(\"contrast fixed\")\nPY\n.venv/bin/python builder.py >/dev/null && .venv/bin/python -m pytest -q 2>&1 | tail -2\n.venv/bin/python - <<'PY'\ndef lin(c):\n    c/=255\n    return c/12.92 if c<=0.04045 else ((c+0.055)/1.055)**2.4\n"
    outcome: passed
    passed: 17
    failed:
    detail: 17 passed
  - command: "cat >> tests/test_build.py <<'EOF'\n\n\n# --- palette -------------------------------------------------------------\n\ndef _relative_luminance(hex_colour: str) -> float:\n    channels = hex_colour.lstrip(\"#\")\n    values = [int(channels[i : i + 2], 16) / 255 for i in (0, 2, 4)]\n    linear = [c / 12.92 if c <= 0.04045 else ((c + 0.055) / 1.055) ** 2.4 for c in values]\n    return 0.2126 * linear[0] + 0.7152 * linear[1] + 0.0722 * linear[2]\n\n\ndef _contrast(foreground: str, background: str) -> float:\n    a"
    outcome: passed
    passed: 19
    failed:
    detail: 19 passed
  - command: "cd /home/fs42/Development/pioneerone && python3 - <<'PY'\nimport pathlib\n\ns=pathlib.Path(\"content/site.toml\"); t=s.read_text()\nt=t.replace('heading = \"If Pioneer One continues, you\\'ll hear it here first.\"',\n            'heading = \"If Pioneer One continues, you’ll hear it here first.\"')\nt=t.replace('note = \"Occasional. Only when there is something to say.\"',\n            'note = \"Occasional, and only when there is something to say.\"')\ns.write_text(t)\n\ne=pathlib.Path(\"content/episodes.toml\"); t=e.r"
    outcome: passed
    passed: 19
    failed:
    detail: 19 passed
  - command: rm -rf /tmp/claude-1000/-home-fs42/de5f5c4d-1e9e-40f5-8d55-6d1faa5219ed/scratchpad/shots && .venv/bin/python builder.py && .venv/bin/python -m pytest -q 2>&1 | tail -2 && timeout 240 .venv/bin/python shoot.py /tmp/claude-1000/-home-fs42/de5f5c4d-1e9e-40f5-8d55-6d1faa5219ed/scratchpad/shots 2>&1 | tail -4 && echo "--- weight ---" && du -sh site/ && du -sb site/ | cut -f1
    outcome: passed
    passed: 19
    failed:
    detail: 19 passed
  - command: "python3 - <<'PY'\nimport pathlib\na=pathlib.Path(\"templates/archive.html\"); t=a.read_text()\nt=t.replace('<a href=\"{{ rel }}#signup-h\">Be told when it opens &rarr;</a>',\n            '<a href=\"{{ rel }}#signup-h\">How to hear when it opens &rarr;</a>')\na.write_text(t)\nprint(\"archive wording updated\")\nPY\ncat >> tests/test_build.py <<'EOF'\n\n\n# --- the signup section --------------------------------------------------\n\n@pytest.mark.parametrize(\n    \"newsletter, expected\",\n    [\n        ({\"action\": \"https"
    outcome: passed
    passed: 20
    failed:
    detail: 20 passed
  - command: (nohup .venv/bin/python serve.py 8791 >/dev/null 2>&1 &); sleep 2; .venv/bin/python -m pytest -q 2>&1 | tail -3
    outcome: passed
    passed: 25
    failed:
    detail: 25 passed
  - command: "python3 - <<'PY'\nimport pathlib\nr=pathlib.Path(\"README.md\"); t=r.read_text()\nt=t.replace(\"\"\"**Turning on the mailing list** is one line: set `newsletter.action` in\n`content/site.toml` to any endpoint that accepts a POSTed `email` field. Until\nthen the section renders a mailto link, which needs no service and cannot\nquietly break.\"\"\",\n\"\"\"**The \"stay in touch\" section** offers whichever of three things is actually\ntrue, decided by `newsletter_mode()` in `builder.py`: a form if\n`newsletter.action` "
    outcome: passed
    passed: 25
    failed:
    detail: 25 passed
context:
  tokens: 357704
  window: 1000000
  window_confidence: observed
provenance:
  git:
    source: observed
    at: '2026-08-15T19:32:24Z'
  files_modified:
    source: observed
    at: '2026-08-15T19:32:24Z'
  tests:
    source: observed
    at: '2026-08-15T19:32:24Z'
  context:
    source: observed
    at: '2026-08-15T19:32:24Z'
---

# Session — pioneerone — 2026-08-15

_Trigger: idle · producer: devtracker_

## Completed

_None recorded._

## Outstanding

_None recorded._

## Files modified

- content/episodes.toml
- static/style.css
- templates/base.html
- templates/story.html
- templates/archive.html
- static/site.js
- assets-src/SOURCES.md
- STORY-SOURCES.md
- tests/test_build.py
- README.md
- serve.py
- shoot.py
- prepare_assets.py
- tests/test_interaction.py
- PARKING_LOT.md
- /home/fs42/Development/workspace/reports/pioneerone-mvp-2026-08-15.md
- content/site.toml
- builder.py
- templates/index.html
- deploy/README.md

## Tests

| Command | Outcome | Passed | Failed |
|---|---|---|---|
| `cat > requirements.txt <<'EOF'
# The build needs a template engine and nothing else.
Jinja2==3.1.6
EOF
cat > requirements-dev.txt <<'EOF'
-r requirements.txt
# Deriving images from assets-src/ (prepare_assets.py) — not needed to deploy.
Pillow==10.2.0
# Rendering the built site at device sizes (shoot.py) — not needed to deploy.
playwright==1.49.0
pytest==8.3.4
EOF
cat > .gitignore <<'EOF'
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Generated by builder.py on every build, including at deploy time` | **unknown** | None | None |
| `tail -12 /tmp/claude-1000/-home-fs42/de5f5c4d-1e9e-40f5-8d55-6d1faa5219ed/tasks/b1pn98q3e.output 2>/dev/null; echo "---tests---"; .venv/bin/python -m pytest -q 2>&1 | tail -25` | **failed** | 9 | 3 |
| `.venv/bin/python -m pytest tests/test_build.py::test_every_referenced_asset_exists -q 2>&1 | grep -A15 "referenced but not built"` | **failed** | None | 1 |
| `python3 - <<'PY'
import pathlib

# --- real bug: autoescape mangled the JSON-LD into &#34; entities ---
b=pathlib.Path("builder.py"); t=b.read_text()
t=t.replace('''            "sameAs": [watch["channel_url"], watch["archive_org_url"]],
        },
        indent=2,
    )''','''            "sameAs": [watch["channel_url"], watch["archive_org_url"]],
        },
        indent=2,
    ).replace("<", r"\\u003c")  # cannot break out of the <script> that carries it''')
b.write_text(t)

i=pathlib.Path("t` | **passed** | 12 | None |
| `python3 - <<'PY'
import pathlib

# builder: expose the portrait srcset alongside the landscape one
b=pathlib.Path("builder.py"); t=b.read_text()
t=t.replace('    hero_widths = image_widths("hero")',
            '    hero_widths = image_widths("hero")\n    hero_portrait_widths = image_widths("hero-portrait")')
t=t.replace('            hero_srcset=srcset("hero", hero_widths, rel),',
            '            hero_srcset=srcset("hero", hero_widths, rel),\n            hero_portrait_srcset=srcset("her` | **passed** | 12 | None |
| `python3 - <<'PY'
import pathlib, re

# The mobile hero is now a landscape band, so the portrait crop has no user.
p=pathlib.Path("prepare_assets.py"); t=p.read_text()
t=t.replace("""    # A phone hero is roughly 393x800. object-fit: cover on a 16:9 frame at that
    # shape shows about a quarter of the image's width — enough zoom that the
    # patch stops being a patch and becomes a red smear. A 3:4 crop is close
    # enough to the viewport that the frame survives, so phones get their own.
   ` | **passed** | 12 | None |
| `.venv/bin/python -m pytest tests/test_interaction.py -q 2>&1 | tail -30` | **error** | None | None |
| `cd /home/fs42/Development/pioneerone && .venv/bin/python -m pytest tests/test_interaction.py -q 2>&1 | tail -30` | **passed** | 5 | None |
| `python3 - <<'PY'
import pathlib
c=pathlib.Path("static/style.css"); t=c.read_text()
old="  --paper-faint: #6f6b66; /* captions, metadata */"
new="  --paper-faint: #817d78; /* captions, metadata — 4.5:1 on both grounds */"
assert old in t
t=t.replace(old,new)
c.write_text(t); print("contrast fixed")
PY
.venv/bin/python builder.py >/dev/null && .venv/bin/python -m pytest -q 2>&1 | tail -2
.venv/bin/python - <<'PY'
def lin(c):
    c/=255
    return c/12.92 if c<=0.04045 else ((c+0.055)/1.055)**2.4
` | **passed** | 17 | None |
| `cat >> tests/test_build.py <<'EOF'


# --- palette -------------------------------------------------------------

def _relative_luminance(hex_colour: str) -> float:
    channels = hex_colour.lstrip("#")
    values = [int(channels[i : i + 2], 16) / 255 for i in (0, 2, 4)]
    linear = [c / 12.92 if c <= 0.04045 else ((c + 0.055) / 1.055) ** 2.4 for c in values]
    return 0.2126 * linear[0] + 0.7152 * linear[1] + 0.0722 * linear[2]


def _contrast(foreground: str, background: str) -> float:
    a` | **passed** | 19 | None |
| `cd /home/fs42/Development/pioneerone && python3 - <<'PY'
import pathlib

s=pathlib.Path("content/site.toml"); t=s.read_text()
t=t.replace('heading = "If Pioneer One continues, you\'ll hear it here first."',
            'heading = "If Pioneer One continues, you’ll hear it here first."')
t=t.replace('note = "Occasional. Only when there is something to say."',
            'note = "Occasional, and only when there is something to say."')
s.write_text(t)

e=pathlib.Path("content/episodes.toml"); t=e.r` | **passed** | 19 | None |
| `rm -rf /tmp/claude-1000/-home-fs42/de5f5c4d-1e9e-40f5-8d55-6d1faa5219ed/scratchpad/shots && .venv/bin/python builder.py && .venv/bin/python -m pytest -q 2>&1 | tail -2 && timeout 240 .venv/bin/python shoot.py /tmp/claude-1000/-home-fs42/de5f5c4d-1e9e-40f5-8d55-6d1faa5219ed/scratchpad/shots 2>&1 | tail -4 && echo "--- weight ---" && du -sh site/ && du -sb site/ | cut -f1` | **passed** | 19 | None |
| `python3 - <<'PY'
import pathlib
a=pathlib.Path("templates/archive.html"); t=a.read_text()
t=t.replace('<a href="{{ rel }}#signup-h">Be told when it opens &rarr;</a>',
            '<a href="{{ rel }}#signup-h">How to hear when it opens &rarr;</a>')
a.write_text(t)
print("archive wording updated")
PY
cat >> tests/test_build.py <<'EOF'


# --- the signup section --------------------------------------------------

@pytest.mark.parametrize(
    "newsletter, expected",
    [
        ({"action": "https` | **passed** | 20 | None |
| `(nohup .venv/bin/python serve.py 8791 >/dev/null 2>&1 &); sleep 2; .venv/bin/python -m pytest -q 2>&1 | tail -3` | **passed** | 25 | None |
| `python3 - <<'PY'
import pathlib
r=pathlib.Path("README.md"); t=r.read_text()
t=t.replace("""**Turning on the mailing list** is one line: set `newsletter.action` in
`content/site.toml` to any endpoint that accepts a POSTed `email` field. Until
then the section renders a mailto link, which needs no service and cannot
quietly break.""",
"""**The "stay in touch" section** offers whichever of three things is actually
true, decided by `newsletter_mode()` in `builder.py`: a form if
`newsletter.action` ` | **passed** | 25 | None |

## Git

- Branch: — → main
- HEAD: — → 879437a

## Context at close

357704 tokens of a 1000000 window (observed).

_Recorded here rather than in CURRENT_STATE.md: context describes the editor, not the project._
